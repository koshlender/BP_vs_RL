# Chapter 4 Ambiguity Resolution

Repository search on 2026-07-21 found no LaTeX thesis source files in this checkout. The implementation therefore uses the Chapter 3 and Chapter 4 thesis excerpts supplied in the task prompt as the available authoritative text, and records unresolved items explicitly instead of presenting them as thesis facts.

| Issue | Thesis evidence | Previous behavior | Corrected behavior | Status |
|---|---|---|---|---|
| Queue constants `k,l,m,n,o,p` | Chapter 4, **Non Cooperative Signal**, defines six symbols in the queue-discretization cases but does not assign independent numeric queue values in the available repository/prompt source. | Treated `0,1,2,3,4,5` as if they were the queue constants. | `k,l,m,n,o,p` are represented as symbolic labels with separate categorical IDs for computation and explicit queue intervals. | Still unresolved |
| Queue-discretization gaps at `11,21,31,41` | Chapter 4, **Non Cooperative Signal**, lists consecutive 10-vehicle categories with strict lower-bound notation that omits exact integer boundaries. | Comments called this ambiguous, but the mapping did not explicitly prove every integer belongs to exactly one interval. | Uses the only consecutive integer partition consistent with the stated buckets: `0..10`, `11..20`, `21..30`, `31..40`, `41..50`, `>50`; tests exhaustively verify uniqueness over non-negative integer samples. | Necessary interpretation of ambiguous notation |
| Cycle time `T` and yellow time `Y` | Chapter 3, **Action/Simulation**, explicitly states `T=80` seconds and total yellow time `Y=16` seconds. Chapter 4 defines `sum_j g_j(t)=T-Y` but does not restate values in the available repository/prompt source. | Chapter 4 action code locally used `80` and `16` as defaults. | Shared constants define `T=80`, `Y=16`, and `T-Y=64` once. Chapter 3 and Chapter 4 code import the same configuration to prevent divergence. | Resolved by cross-chapter evidence |
| Zero total queue in Chapter 4 reward | Chapter 4, **Reward**, defines `R(t)=1/sum_i Q_i(t+1)` but available source does not define the zero-denominator case or prove it cannot occur. | Returned `math.inf`, which can corrupt TD targets and Q-values. | Separates the thesis equation from a configurable zero-queue simulation policy. Positive denominator uses the thesis equation exactly; zero denominator defaults to finite safeguard `0.0` and marks that the thesis equation was not used. | Still unresolved |
